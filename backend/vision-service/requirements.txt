# torch is NOT listed here on purpose — install it separately, matched to
# your GPU's CUDA version (see README). Installing it via this file would
# silently overwrite a correctly-matched CUDA build with a generic one.
transformers>=5.13
fastapi>=0.139
uvicorn>=0.49
pillow>=12
numpy>=2
